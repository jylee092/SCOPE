# =============================================================
# SCOPE artifact -- Python dependencies
#
# Tested with Python 3.11 on Windows 11 and Ubuntu 22.04.
# CPU-only inference (the timing harness in §7.7 uses no GPU).
# =============================================================

# Core data processing
pandas>=2.0
numpy>=1.24
networkx>=3.1

# NLP / embeddings
sentence-transformers>=2.5
transformers>=4.30
torch>=2.1

# Retrieval
faiss-cpu>=1.7
rank-bm25>=0.2.2

# Graph baseline (MAGIC re-implementation)
torch-geometric>=2.4

# Sigma rule evaluator (Sigma baseline)
pysigma>=0.10
pysigma-backend-elasticsearch>=1.0  # optional, for cross-checking
pyyaml>=6.0

# Community detection (SHIELD re-implementation)
python-louvain>=0.16

# Scientific
scikit-learn>=1.3
scipy>=1.11

# Plotting (paper figures)
matplotlib>=3.7

# LLM API (only if regenerating cache; not needed for reproduction)
google-generativeai>=0.4

# System monitoring (Q5 efficiency timing)
psutil>=5.9

# Progress / utilities
tqdm>=4.66
